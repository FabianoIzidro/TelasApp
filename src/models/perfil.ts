export class Perfil{

    public constructor(public nome: string, public email: string, 
        public cpf: string, public telefone: string, public rua: string, public cep: string, 
        public numero: string, public bairro: string, public cidade: string, public id?: string ){}
}